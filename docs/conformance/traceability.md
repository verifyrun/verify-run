# Traceability matrix — Decision Replay Conformance Profile v1

Generated by `tools/build_conformance_manifest_of_record.py`. Every MUST and MUST NOT is
traced from the normative statement to the fixtures that witness it and, where the
violation is one an implementation could fake, to the hostile stub that proves the runner
detects it.

A requirement with no witness is decoration. A fixture asserting something the contract
never stated would be the runner inventing a standard. Neither is present below, and
`tests/test_conformance.py` fails the build if either appears.

| Requirement | Level | Positive | Negative | Hostile stub | In machine profile |
|---|---|---|---|---|---|
| DR-1.1 | MUST | allow-with-execution | missing-candidate-body, missing-rulebook-body, missing-snapshot-body | — | yes |
| DR-1.2 | MUST NOT | allow-with-execution | — | — | yes |
| DR-1.3 | MUST NOT | allow-with-execution | — | verifies-database-verdict-only | yes |
| DR-2.1 | MUST | allow-with-execution, allow-without-execution, block, hold | — | — | yes |
| DR-2.2 | MUST | allow-with-execution-repeat | — | — | yes |
| DR-2.3 | MUST NOT | hold | — | maps-hold-to-block | yes |
| DR-2.4 | MUST NOT | — | altered-nested-result | maps-error-to-block | yes |
| DR-2.5 | MUST NOT | — | 24 bundles | defaults-allow | yes |
| DR-3.1 | MUST | allow-with-execution | authorization-not-the-one-named | — | yes |
| DR-3.2 | MUST | allow-with-execution | — | — | yes |
| DR-3.3 | MUST NOT | block, hold | — | — | yes |
| DR-3.4 | MUST | allow-with-execution | — | — | yes |
| DR-3.5 | MUST | — | — | — | no |
| DR-4.1 | MUST | allow-with-execution | — | — | yes |
| DR-4.2 | MUST | allow-with-execution | wrong-candidate-body, wrong-rulebook-body, wrong-snapshot-body | — | yes |
| DR-4.3 | MUST | allow-with-execution | altered-nested-result | — | yes |
| DR-4.4 | MUST | allow-with-execution | altered-key-id, altered-key-version, no-signature | — | yes |
| DR-4.5 | MUST | — | 9 bundles | ignores-altered-candidate-binding | yes |
| DR-4.6 | MUST | allow-with-execution | untrusted-key-valid-signature | verifies-database-verdict-only | yes |
| DR-5.1 | MUST | — | altered-key-id, altered-key-version, unknown-receipt-key | trusts-unknown-key | yes |
| DR-5.2 | MUST | — | untrusted-key-valid-signature | trusts-unknown-key | yes |
| DR-5.3 | MUST | — | 4 bundles | — | yes |
| DR-5.4 | MUST | — | unsupported-replay-mode | — | yes |
| DR-5.5 | MUST | — | unknown-field | — | yes |
| DR-6.1 | MUST | allow-with-execution, allow-without-execution, block, hold | — | signs-but-cannot-recompute | yes |
| DR-6.2 | MUST | allow-with-execution | authorization-not-the-one-named | — | yes |
| DR-6.3 | MUST NOT | allow-with-execution | — | — | yes |
| DR-6.4 | MUST NOT | allow-with-execution | — | — | yes |
| DR-6.5 | MUST NOT | allow-with-execution | — | rejects-expired-allow | yes |
| DR-6.6 | MUST | allow-with-execution | — | rejects-expired-allow | yes |
| DR-6.7 | MUST | — | 6 bundles | — | yes |
| DR-6.8 | SHOULD | allow-retired-key | — | — | yes |
| DR-7.1 | MUST | allow-with-execution | — | — | yes |
| DR-7.2 | MUST | — | altered-execution-acknowledgment | ignores-altered-acknowledgment | yes |
| DR-7.3 | MUST NOT | allow-without-execution | — | — | yes |
| DR-8.1 | MUST | — | unsupported-replay-mode | — | yes |
| DR-8.2 | MUST NOT | — | 6 bundles | — | yes |
| DR-9.1 | MUST | — | — | — | no |

## Requirements without a fixture

- **DR-3.5** — see the normative document, which states why.
- **DR-9.1** — see the normative document, which states why.

## Statements

- **DR-1.1** (MUST) An artifact MUST identify or contain the exact declared inputs needed to recompute the
- **DR-1.2** (MUST NOT) Recomputation MUST NOT require reacquiring evidence from a live source.
- **DR-1.3** (MUST NOT) Recomputation MUST NOT require a mutable database row, an unrecorded model response,
- **DR-2.1** (MUST) For declared inputs and a stated profile, settlement MUST produce exactly one terminal
- **DR-2.2** (MUST) The same declared inputs MUST produce the same terminal on repetition.
- **DR-2.3** (MUST NOT) HOLD MUST NOT be reported as BLOCK or as ALLOW. HOLD means the rulebook could not
- **DR-2.4** (MUST NOT) ERROR MUST NOT be reported as a decision terminal. ERROR is a preparation or validity
- **DR-2.5** (MUST NOT) An implementation MUST NOT default to ALLOW when settlement cannot be completed.
- **DR-3.1** (MUST) Where the terminal is ALLOW and an authorization exists, the artifact MUST name it, and
- **DR-3.2** (MUST) An authorization MUST declare a validity interval whose end is after its start.
- **DR-3.3** (MUST NOT) BLOCK, HOLD, and ERROR MUST NOT carry an authorization or an execution record.
- **DR-3.4** (MUST) An authorization MUST carry a single-use identifier, unique within the implementation's
- **DR-3.5** (MUST) An implementation that exposes spending of a supplied authorization MUST verify the
- **DR-4.1** (MUST) The artifact MUST be canonical under its declared canonicalization profile.
- **DR-4.2** (MUST) The artifact MUST bind the recorded inputs by digest.
- **DR-4.3** (MUST) The artifact MUST bind the terminal result.
- **DR-4.4** (MUST) The artifact MUST be signed under a declared algorithm and key identity.
- **DR-4.5** (MUST) Altering any protected field MUST cause verification to fail.
- **DR-4.6** (MUST) Verification MUST require only the artifact and an independently supplied public trust
- **DR-5.1** (MUST) A verifier MUST refuse an artifact whose signing key identity is unknown to the
- **DR-5.2** (MUST) A verifier MUST refuse an artifact bearing a cryptographically valid signature from a
- **DR-5.3** (MUST) A verifier MUST refuse a missing, malformed, or copied signature.
- **DR-5.4** (MUST) A verifier MUST refuse an unsupported replay mode.
- **DR-5.5** (MUST) A verifier MUST refuse an unknown protected field where the profile forbids one.
- **DR-6.1** (MUST) A replayer MUST recompute the recorded terminal from the recorded inputs.
- **DR-6.2** (MUST) A replayer MUST verify the recorded bindings.
- **DR-6.3** (MUST NOT) A replayer MUST NOT reacquire evidence.
- **DR-6.4** (MUST NOT) A replayer MUST NOT execute the action.
- **DR-6.5** (MUST NOT) A replayer MUST NOT require the recorded authorization to be presently spendable.
- **DR-6.6** (MUST) Recomputation MUST continue to succeed after the recorded authorization's validity
- **DR-6.7** (MUST) A replayer MUST refuse a missing or substituted recorded input.
- **DR-6.8** (SHOULD) Retired signing keys SHOULD continue to verify artifacts they already signed.
- **DR-7.1** (MUST) Where an artifact carries an execution acknowledgment, it MUST be inside the protected
- **DR-7.2** (MUST) Altering the acknowledgment MUST cause verification to fail.
- **DR-7.3** (MUST NOT) An artifact without an acknowledgment MUST NOT be presented as evidence that the action
- **DR-8.1** (MUST) An implementation MUST refuse, or return ERROR, for malformed declared inputs, invalid
- **DR-8.2** (MUST NOT) An implementation MUST NOT normalize a protected artifact into acceptance. Repairing,
- **DR-9.1** (MUST) An implementation MUST declare which profile versions it accepts, which artifact
