{
  "description": "forbidden regex class",
  "kind": "registry",
  "documents": [
    {
      "$id": "x:1",
      "type": "string",
      "pattern": "^\\d+$"
    }
  ],
  "expected": {
    "outcome": "ERROR",
    "reason_code": "schema_registry_invalid"
  }
}
